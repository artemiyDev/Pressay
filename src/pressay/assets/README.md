# Pressay brand assets

- `app-icon.svg` is the primary application icon.
- `logo-mark.svg` is the flat microphone mark.
- `wordmark.svg` is the wide brand artwork used in documentation.
- Windows and macOS installers generate native `.ico` and `.icns` files.
